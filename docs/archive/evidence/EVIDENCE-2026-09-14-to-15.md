# MVP 运行证据

## 账户视觉修订（2026-09-15）

用户否定上一版姓名挤紧与霓虹方框效果后，仅修改 workspace.ts 账户标记和 style.css 对应样式：姓名保持 15px、字元间 4px 且不收缩；正方形头像采用冷蓝/金色纹理流光、余烬和主体遮罩，移除横穿头像的白色斜线及双层霓虹框。视觉参考实际查看的[斩神动画官微宣传画面（新浪镜像）](https://www.sina.cn/news/detail/5332510776755081.html)，没有复制远程素材到应用；本次未观看完整动画特效片段，风格效果仍由用户评价。

主控在正式 8765、628×771 浏览器实测：头像为 38×38，姓名三个字元与头像中线均 y=721，姓名宽 57.5px，无页面横向溢出；Enter 可展开和收起账户菜单，控制台无 error。npm run build（含 TypeScript）通过；保留既有编辑器大于 500kB 提醒。system_reviewer 只读两轴审查无阻塞项，指出触发器缺少用途名称；已添加“蒸🐮🐸，账户菜单”，重建后实际 AX 树核验该名称与 collapsed 状态。减少动态效果规则覆盖三类动画，未额外模拟系统设置。代码与样式均经真实浏览器查看，未写业务数据。

本文件以下保留早期MVP和参考读取的历史证据，21项测试是当时结果。任务工作台当时的34项回归、Journey浏览器路径、冲突/反馈/导出及正式服务证据见 [任务工作台首试](../../execution/TASK-WORKSPACE.md#实际验收2026-09-14)；不把本页旧“最终”结论视为当前完整产品完成。

2026-09-14。原 MVP 测试仅使用 `fixtures/scenarios.json` 虚构资料，数据在代码外临时目录。下述追加的参考读取由用户另行指定 URL 授权，和测试数据分开；不将线上个人简历写入仓库或 fixtures。本文记录证据，当前计划与状态仅见 STATUS。

## 参考读取与产品纠偏（2026-09-14）

- [本地 A 方案](http://127.0.0.1:8788/?variant=A)：实际浏览首页、已完成虚拟案例及“我的材料”。导航为今天、正在推进、我的材料、练习与收获、足迹；页面明确是规则模拟的隔离原型。“我的材料”明确链接妙搭工作台，并写明编辑、排版、PDF 回到实际工作台。不能把该原型视为真实 AI 实现证据。
- [妙搭工作台](https://fcnxusey6bqe.feishuapp.com/app/app_17dspw78s10)：浏览 A4 纸面、编辑入口、历史版本列表。实际调用只读 WebMCP `read_resume_document`，读到结构化分区、条目 ID 和格式；页面还声明全量替换工具，本轮未调用。两次只读文档返回比较一致。历史版本的恢复/删除、保存版本和 PDF 导出按钮未执行，本次不重新声称其写入/导出可靠性已验收。`lark-cli apps +get --app-id app_17dspw78s10 --as user` 成功读取既有 full_stack 应用元数据；未取得源码。
- [career-ops](https://github.com/career-ops-hq/career-ops)及其 [Architecture](https://github.com/career-ops-hq/career-ops/blob/main/ARCHITECTURE.md)：读取流程与架构说明。可借鉴岗位评估、材料生成、跟踪和人工决策的分工。其文件正本/SQLite 派生索引属于该仓库自己的数据约定，不自动替换本项目契约；未安装或运行该项目。
- [Greenroom](https://greenroom.ungetsu.net/#work)：网页抓取失败后，浏览器成功读取产品内容、嵌入的机会管线和简历工作室。看到阶段/下一步/截止信息，独立模块导航，基础/岗位/语言版本入口、导入和模板入口，以及逐条审阅修改的产品说明。仅观察演示，不声称验证了它的 AI、导入或完整写入流程。

上述证据揭示此前简历验收漏检“沿用既有工作台”这一产品要求。已有后端测试与 PDF 文件验证仍是技术事实，不能证明当前纯文本 UI 符合用户预期。修正后的产品要求见 01-product.md；不在证据页另存产品正本。

## 工具链与协作

初始 `python3 -B scripts/verify_bundle.py`：PASS，28 payload files。初始非 Git 仓库，已初始化；Python 3.9.6、Node v22.23.2、npm 10.9.8。Python direct+transitive 依赖固定于 requirements.lock，前端依赖由 package-lock.json 固定。

后台工具显式接受 gpt-5.6-luna / medium。已收到 Provider/Artifact、初版 frontend、独立 review 的结果。初版 frontend 自报存在投递缺项，主控未视为完成；旧执行者不再出现在当前宿主可调度列表后，已重派 frontend_finish。宿主可能显示 Subagent 聊天面板；没有调用 create_thread，没有额外分支/worktree。主控实际模型/reasoning 无运行配置可读，无法核验。

## 自动化验证

`PYTHONPATH=src .venv/bin/pytest tests -q`：21 passed（本轮恢复后实际复验）。

- Context：旧事实修订后实际 Provider payload 只含新资料；不会混入反馈；只有 fresh system/user messages。非法来源引用会失败且不改正本。
- Revision：旧 expected_revision 返回冲突；分析中修改资料结果 stale；预览后更改资料拒绝发送；手改简历让待应用提案 stale。
- Proposal：确认后同事务写草稿与 applied_result，重复应用不重复修改；并发手改不会被旧提案覆盖。
- History：正式版本与实际 PDF 独立保存；投递关联岗位/版本/PDF并保存事件快照，后续资料/草稿/岗位删除不会修改事件。错配岗位/版本拒绝。
- Idempotency：同一分析 key 只调用一次 Provider；同一投递 key 只生成一次事件；并发PDF导出返回同一 artifact且只保留一文件。
- Feedback：原话包括空白不变、补充追加、JSON/Markdown 导出；不会进入 Context。
- Local：Origin/Host/自定义写入 header 限制；新 Store 读取已存对象；SQLite 快照备份后在新隔离目录恢复，PDF字节一致。
- Provider：未配置真实模式仍能启动和手动编辑，调用返回503并记录失败；Real adapter请求体通过模拟HTTP client捕获，无真实模型调用。

关键 TDD 证据：core 未实现时测试 collection失败；新增重复分析/并发PDF测试时2 failed；反馈原文空白测试1 failed。实施后相应测试通过。不是仅靠静态代码宣称正确。

`python -m compileall -q src scripts/run.py scripts/backup.py`：通过。

## 两轴审查

规格轴已处理：Provider返回格式错配；默认未配置影响独立手动链；JD不应写成本人简历事实；反馈原文被strip；简历后续编辑导致旧提案不可应用却未显示过期；初版前端缺投递表单/正确预览。最后一项仍在前端验收中。

工程轴已处理：重复分析会重复收费调用；并发PDF重复文件；字体fallback缓存后续失败；artifact根目录symlink逃逸；实际请求无旧会话；缺密钥错误脱敏。初版 frontend 自报构建不能证明完整主链，已要求重做表单/冲突处理后实测。

## 浏览器与实际文件

隔离服务 `CAREER_AI_PROVIDER=test CAREER_DATA_DIR=/tmp/career-os-mvp-browser .venv/bin/python scripts/run.py --port 8766 --no-browser`。实际浏览器已打开首页、填写 fixtures 个人资料并保存，页面显示 revision 1。其余主链与PDF视觉待新版前端整合后记录。

## 真实能力边界

未检测到 CAREER_AI_API_KEY / OPENAI_API_KEY / CAREER_AI_MODEL。真实岗位分析与真实简历适配尚未验收；TestProvider只证明流程与隔离。不得称真实AI闭环完成。配置步骤位于 src/workbench/README.md。

## 最终整合验收（18:37–18:45）

前端已由主控完成交互整合，Luna 完成样式并作只读审查。`npm --prefix frontend run typecheck` 与 `npm --prefix frontend run build` 实际通过；Vite 6.4.3 构建本地静态资源。未加载远端字体。页面浏览器 console error/warn 检查为空。

实际浏览器操作：

1. 个人资料保存并显示 revision 1；表单添加虚构公司甲 JD。
2. 岗位分析先展示 taskKind=job、profile/JD revision 1 和完整 sources，再点击测试模式发送，返回 job 字段与可展开实际 payload。
3. 从岗位进入独立草稿，taskKind=resume 也先预览；提案返回后工作稿仍空，查看 before/after、确认应用后变为版本1，正本不变。
4. 人工编辑成简历格式，后台自动保存为版本2；编辑时打开全局反馈，保存反馈后仍停留简历且正文不丢。
5. 保存正式版本并实际生成 PDF（32636 bytes、1页 A4）。`pdfinfo` 读取成功，`pdftoppm -scale-to 1200 -png -singlefile` 渲染后主控查看图片，中文清晰无黑块/裁切。macOS Preview 实际打开 PDF，AX 文本与所保存的简历正文一致。
6. PDF 内置浏览器预览失败，因此增加下载兼容入口。浏览器实际触发 PDF download；HTTP 验证 Content-Disposition=attachment，文件以 %PDF 开头，哈希与登记一致。
7. 从该版本打开投递表单，确认岗位、正式版本、PDF、正常日期时间控件和中文状态，保存实际投递事件，更新状态为“面试中”。此为虚构验收事件，未执行任何对外投递。
8. 反馈页显示原话，追加说明后两者并列保留；JSON 和 Markdown 均触发浏览器 download。另用测试 PDF 的渲染图片作为可选截图上传，保存后出现可访问的截图链接。
9. 双窗口同时打开 profile revision1：第二窗口先保存 revision2，旧窗口再保存时出现“你的未保存内容 / 服务器当前内容”差异，内容未丢。显式基于最新版本继续编辑，合并后保存 revision3。
10. 添加虚构公司乙并进入简历，显示版本0空稿且无甲公司的投递。切回甲时原稿和投递恢复。删除甲岗位后其退出活动列表，仍可查看历史简历/PDF/面试中状态，分析按钮禁用。

实际进程重启：停止测试/生产进程 65452/65776，重新启动为66894/66895。重启前将合成状态保存到隔离目录；重启后比较 profile/jobs/resumes/versions/artifacts/applications/feedback/runs 全部相同，所有附件 HTTP 字节 SHA-256 与登记一致。浏览器重载后读取工作稿版本2、正式版本、历史 PDF、面试中状态，个人资料更新导致旧分析明确显示过期。

生产入口 8765 已实际启动并打开首页：当前资料为空、岗位/投递为0、显示“真实 AI · 未配置”。没有写入测试样例。生产与测试目录不同。

最终回归：`npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`.venv/bin/pytest -q` 均通过，后者21 passed。后续仅增加了导航恢复及保存串行化的小修复，重新typecheck/build通过。真实模型从未调用，保留外部配置 Gate。

最终两轴结论：当前 MVP 独立能力符合预览/确认/不改历史/反馈隔离要求；工程关键数据和文件路径已通过测试及浏览器验证。当前局限集中记录 STATUS，未以任职、语音、自动采集等未来功能充数。


## 事实闭环整改（2026-09-15）

### 改动范围与两轴验收

前批已完成鼠标新增及共用投递。本批根目录原工作树增量完成editor选材/来源、profile整理、journey更正/候选和主页面接线；保留其他批次已有改动。临时Luna medium后台实现限定文件，主控审查服务边界并实际跑用户路径。无新用户任务、分支或worktree。

规格轴通过：长期事实属于Wiki，简历表达有来源但不倒写正本；版本/投递固定；原始记录与复盘更正分开；候选确认前不进Context；基础身份与目标/约束归属收敛。规范轴通过：原子CAS与幂等、跨scope校验、来源防伪、旧格式兼容、保留输入和独立审查。独立审查提到删除表达后的重新选材产生两条ref，裁决为原removed历史＋新current表达，已有专项测试。

### 自动化与构建

- `PYTHONPATH=src .venv/bin/pytest tests -q`：57 passed in 2.47s。
- `npm --prefix frontend run typecheck`、`npm --prefix frontend run build`、`git diff --check`通过。Vite仍提示PDF相关包超过500kB，非失败。
- 新测试test_fact_resume覆盖事实来源、工作稿自由表达/来源防伪、Wiki更新后版本/投递冻结、scope及revision、删除表达后重选、研究记录候选确认前后实际Provider payload。test_profile_ownership覆盖旧文本归档/唯一profile、确认前后Context及基础选入。test_record_candidates覆盖原文更正、幂等回放顺序、固定投递继承、范围提升和完整导出。

### 真实浏览器路径（隔离虚构数据）

环境 `/tmp/career-os-fact-loop-0915`、8772、TestProvider；首次空白稿专项 `/tmp/career-os-fact-blank-0915`、8773。前端先构建到 `/tmp/career-fact-loop-dist`，与正式数据隔离。UI实际操作：

1. 旧混合profile原文只读→明确整理姓名/邮箱和作息约束候选→查看待确认→确认Wiki；跨页基础字段未保存buffer回显并保存。
2. 创建虚构晨光研究室/产品运营机会，登记知识库原件→候选→确认项目；从机会简历进入编辑器带job_id，选项目和基础字段；当前稿显示林示例、邮箱、二十篇文档，来源显示Wiki v1/profile v2。
3. 保存“晨光产品运营-事实版”及浏览器实际PDF；返回原机会简历tab→关联版本→登记隔离测试渠道投递。
4. 面试记录关联该投递→更正为事后复盘→原话/更正全文并列历史；明确选段及跨任务复用→个人候选→确认。未确认时读取Context无该候选，确认后UI选择并预览、返回调整指令、实际调用TestProvider。
5. 实际payload只含基础身份、当前JD、强制个人约束及明确勾选的确认能力，不含完整面试原话、旧混合profile或未选项目原件。
6. 新确认能力选回简历技能；带未保存表达先保存后导入，一次成功；撤销/重做/保存、技能与经历/项目鼠标新增、教育Enter新增均回归。首次空白稿另测一次导入成功，无409。
7. 恢复原正式版本；修改Wiki项目为二十一篇后，简历来源显示“有新版本”，工作稿和旧投递仍保持二十篇；恢复/事实修订/实际进程重启后投递及PDF字节不变。
8. 任职事项选段默认留在该工作卡的pending候选，只保存允许复用文本，不复制完整内部原话。来源按钮可以回到记录与历史。全局页仅指向机会/工作卡创建。

浏览器发现并修复首用缺陷：request.expected_revision原先在flushSave前取值，空白或脏稿首次选材会409。修复后用空白稿与脏稿实际重测。另修正投递名称读取resume_snapshot、机会tab地址同步、来源profile专用链接、导入表单锁与同请求重试、撤销和并发保留来源refs。

固定投递 `1789ab24-868d-4659-b8d3-3416df7e8a4b`，正式版本 `503803e6-735b-462b-b8fe-5dc491cc905a`；浏览器PDF 102894 bytes，sha256 `a1f53bd107e02169110f43cc8996674e0fc238e6c50ef7b22121e427286b8ed8`。完整投递JSON sha256 `8fdc20a76b100bc8c2a4fcc66c14ba468a5c3f67ed13f41df1df0b3b7aa0b51d`（JSON sort_keys默认ASCII编码）。隔离环境实际停止96247、重启96996后，current12/revisions24/records23/applications1与1附件摘要一致，页面重新打开能回看原投递。测试PDF留在 `/tmp/career-fact-loop-browser.pdf`。

### 正式本地服务更新

备份：`/Users/frog/Library/Application Support/CareerOS-backups/career-backup-2bd2353c-2f55-48bb-a267-3ce701822fb1`。

正式构建后停止94183，启动97093监听127.0.0.1:8765。index/editor HTML及其JS/CSS响应字节等于frontend/dist；主JS `index-BV8FAGSw.js`，编辑器 `editor-BE2pucnm.js`，动态编辑实现 `legacy-app-DeAWy6dJ.js`。新materials/sources路由可用；浏览器正式/#profile显示新整理/基础字段界面且无测试标识。

正式目录四表摘要更新前后完全一致：current6、revisions92、records8、applications0，3附件hash一致。没有种入验收样例、自动清洗或迁移用户资料。Provider保持real、configured=false、model空、原base_url；未验证真实AI调用或质量。用户旧页面需刷新。

验收结束已停止8772/8773临时服务并关闭本批验收页；正式8765继续运行。

## 联系图标回归修复（2026-09-15）

用户要求恢复电话/邮箱/微信原图标、GitHub文字标签换风格一致的小猫。根因是profile选材生成新contact ID，原渲染只识别固定ID而回退圆圈。保留原SVG路径，补类型兼容识别；GitHub用Lucide Cat本地SVG，保留ISC授权及链接。仅改前端显示，不迁移用户资料或既有PDF。隔离8774验证生成ID电话、带通用标签邮箱、固定ID微信、小猫链接；截图检查及编辑链接→保存→刷新后四图标和链接保持正确。前端build及diff检查通过。


## 2026-09-15 联系方式填写与用户 GitHub 徽标跟进

本批规格轴：基础资料新增微信、GitHub、多条有标签的个人网页；移除正常保存版本提示和跨页未提交草稿缓存，保留明确保存及服务端修订保护。GitHub改用用户上传的原PNG，保持完整比例；原微信SVG保留，当前简历缺微信时展示可填写入口。保存基础资料后，用户可从Wiki选材主动更新简历，旧版本快照保持原样。

规范轴：新增字段可选，旧三字段客户端不会擦除扩展字段；URL仅接受有效HTTP(S)地址；资料进入简历时保持明确选入和当前来源引用。修复动态链接行删除目标和追加微信被归为链接的问题。本批未自动填写未知个人账号，也未迁移真实资料。

真实验证：58项pytest通过，生产前端构建和git diff --check通过。隔离8774使用虚构资料完成两条网页及微信/GitHub填写→保存回读→未提交修改切页丢弃→选入简历；缺微信场景通过补填入口编辑、保存、重载保留。实际生成88018字节PDF（/tmp/career-contact-badge.pdf），渲染图片人工检查微信图标、用户GitHub徽标与网页排版。

本地8765更新：重启服务PID 99089，继续使用原CareerOS数据目录和未配置密钥的real模式。真实浏览器只读核验基础资料微信/GitHub/个人网页字段、无“已保存 vN”提示，以及简历微信填写入口和GitHub图片src；服务返回的PNG与用户原文件逐字节一致。更新前备份career-backup-17f03f71-e00c-47fa-96a1-ffc4e38257c9；前后四张业务表（current 6、revisions 98、records 9、applications 0）及3个产物哈希一致。未对真实资料执行测试写入。真实模型能力仍未验收，本批不宣称AI全流程完成。
