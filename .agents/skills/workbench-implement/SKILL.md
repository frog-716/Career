---
name: workbench-implement
description: 按已明确的职场中台批次任务实现代码、验证用户路径并返回证据，适用于Astra主控或Luna执行者。
---

先读当前批次、相关Interface文档与允许修改路径。资料/AI任务读docs/02-context-contract.md；界面任务读docs/01-product.md和对应journey；验收读docs/05-acceptance.md相关条目。

1. 验证依赖已满足，检查工作树并保留他人改动。
2. 对数据正确性、不可变历史、幂等、并发和主链行为建立失败测试，再最小实现；低风险视觉修改用实际浏览器检查。
3. 通过同一Interface验证真实用户结果，不用模仿内部代码的断言充数；测试只用隔离虚构数据。
4. 明确测试Provider与真实模型差别，记录未验证接入；不因缺Key就把预设文案称为AI。
5. 返回修改、实际命令结果、未完成项。执行者不越出文件范围，不自行重设产品或声明整批完成。

Astra整合并完成浏览器与相关持久化验证，进入$workbench-review。完成条件是本任务验收已真实通过或明确记录依赖阻塞；不以代码已写完替代验证。
