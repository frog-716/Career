请在当前新项目中搭建“职场中台系统”。项目根目录已放入独立启动包，请以包内需求和本次指令为依据，不回读旧项目或旧线程。

你是主控，使用 Astra（gpt-6-astra）+ high 负责规划、产品取舍、深模块Interface、整合与最终验收；授权你将边界明确的执行任务交给后台临时 Luna（gpt-5.6-luna）+ medium Subagent。请核对实际模型设置，文字自称不能替代模型配置。若当前不是指定模型或宿主不支持子代理，准确说明；继续能够独立完成的工作，不伪称已使用它们。

首先阅读 AGENTS.md，并按它的首轮读取顺序建立理解。其他文档和参考代码按任务读取，不一次性加载所有文件。docs/00-authority.md定义权威顺序：用户确认需求优先，工程建议允许你做最小合理调整。

目标是纯本地运行的个人职场工作台：个人资料沉淀→岗位发现与筛选→公司研究→简历适配→招呼与实际投递→沟通跟进→模拟与真实面试复盘→谈薪/Offer→任职事项、协作问题与阶段总结→确认新经历和下一次求职。界面围绕当前任务，首次打开知道从哪里开始，任务可跳步、回退和并行。

上下文隔离严格遵循 docs/02-context-contract.md：用户资料可自行编辑，AI按明确指令修改或提出待确认差异；每轮分析从当前正本重建资料包，禁止旧修订、旧助手消息、过期摘要和未确认分析重新成为个人事实。历史投递PDF/面试原文仅作为对应事件证据使用。资料在分析期间发生变化时，旧结果标过期，应用前再检查revision。请通过实际请求负载验证，而不是只凭提示词。

请自主推进：

1. 检查本包与运行环境，执行 python3 -B scripts/verify_bundle.py；缺可选能力不阻塞独立工作。保留正常的项目权限设置，不改全局配置。
2. 固定最小工具链和模块Interface，创建 docs/execution/STATUS.md 及首批可执行任务。先给简短架构与实施顺序，然后开始实现，不停在方案讨论。
3. 按 docs/07-roadmap.md 从B00推进至B06。先让当前资料、真实模型入口、本地持久化可靠，再串求职、任职和按轮语音/首个可用岗位源。普通字段、UX、目录和工程取舍由你决定。
4. Luna只接有限任务：给相关文档、输入输出、允许修改路径、验收条件，禁止多个执行者同时修改公共文件。使用 prompts/LUNA-TASK.md 派工；所有结果由你统一审查与实际验证。不要创建额外用户任务、可见分支或worktree作为子代理。
5. 执行 .agents/skills 中的 plan/implement/review 技能。影响资料正确性、不可变历史、事务与主链的行为采用有意义的TDD；页面小修改用浏览器验收，不为假想未来能力堆测试。每批做规格和工程规范两轴审查，处理阻塞问题。
6. 测试只使用 fixtures 的虚构资料，数据目录与代码分离。生产资料从新应用明确导入，不自动扫描旧仓库。模型凭据走服务端/系统配置，不要求我把密钥发到对话；开发模型分工不等于产品已有运行时模型授权。
7. 测试Provider可以验证流程和最新上下文，界面必须标明测试模式。没有真实模型调用就不能宣称AI分析完成；没有实际录音/播放或真实数据源读取就不能宣称语音/搜索完成。缺必要配置时说明精确待办，完成其他独立部分，保留未验收状态。
8. 每批更新STATUS与证据，记录实际启动方式、命令结果、浏览器路径、审查发现和剩余限制。在已有Git仓库中按批提交；根目录还没有Git时可以初始化新仓库，提交前检查凭据与用户数据不会进入版本控制。不自动推送或部署。

只有这些情况需要报告Gate：无法确定的真实资料阻塞任务；不可恢复数据删除/迁移；当前设计使主链接不下去；根本产品取舍；必要外部接入方式无法确认。其余不要逐条让我确认。Gate只暂停依赖部分。

首次完整交付后停止扩展，给出本地启动入口、实际操作全流程、我应重点试的场景、哪些能力已实测、哪些仍缺配置/证据、临时简化和技术债。不要继续开发复杂图谱、云同步、全平台爬虫或全自动对外发送。

现在从包的完整性检查和B00/B01首批任务开始。
